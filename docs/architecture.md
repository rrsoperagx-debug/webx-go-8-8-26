# Arch Go
Chi router + Zerolog JSON + Viper config + modernc sqlite pure Go (CGO_ENABLED=0) + Prometheus + gofpdf
Middleware: ConcurrencyLimiter 512, Timeout 5s, BodyLimit 5MB, SecurityHeaders, CORS, Metrics, Recoverer
1 binary 7MB static, works linux/amd64/arm64, darwin, windows, android
